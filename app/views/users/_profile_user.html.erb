<span class='social-networks'>
    <% if current_user && @user != current_user %>
      <% if current_user.following?(@user) %>
        <%= link_to 'Читаю', unfollow_user_path(@user),
        method: :post,
        class: 'btn btn-primary btn-st btn-follow btn-follow-on',
        data: { toggle: 'tooltip', placement: 'bottom' } %>
      <% else %>
        <%= link_to 'Читать', follow_user_path(@user),
        method: :post,
        class: 'btn btn-primary btn-st',
        data: { toggle: 'tooltip', placement: 'bottom' } %>
      <% end %>
    <!-- <button class = 'btn btn-primary btn-st'>Читать</button> -->
    <% else %>
      <%= link_to profile_edit_path do %>
        Настройки профиля
      <% end %>
    <% end %>
    <h5><%= @user.phone %></h5>
    <p>
      <% if @user.vk_id.present? %>
          <%= link_to "https://vk.com/#{@user.vk_id}", target: :blank do %>
            <img src= '/images/social_vk.png' />
          <% end %>
      <% end %>
      <% if @user.facebook_id.present? %>
          <%= link_to "https://facebook.com/#{@user.facebook_id}", target: :blank do %>
            <img src= '/images/social_facebook.png' />
          <% end %>
      <% end %>
      <% if @user.instagram_id.present? %>
          <%= link_to "https://www.instagram.com/#{@user.instagram_id}", target: :blank do %>
            <img src= '/images/social_insta.png' />
          <% end %>
      <% end %>
    </p>
    <% if current_user == @user && !halffull_contacts?(current_user)%>
      <div class='notification-block notification-more-contact'>
        <%= link_to profile_edit_path do %>Укажите<% end %> больше контактов, чтобы клиенту было легче связаться с Вами!
      </div>
    <% end %>
</span>
<div class='avatar'>
  <div class='img-avatar' style = 'background: url(<%= @user.avatar %>) no-repeat; background-size: cover; background-color: white;'></div>
</div>
<div class='name'>
  <%= @user.name %>
</div>
<% if @user.description.present? %>
  <div class='aboutme'>
    <%= @user.description %>
  </div>
<% else %>
  <% if @user == current_user %>
    <div class='aboutme'>
      <%= link_to profile_edit_path do %>
        Добавить контакты и информацию о себе
      <% end %>
    </div>
  <% end %>
<% end %>
<div class='city'>
  <%= @user.city.try(:name) %>
</div>
